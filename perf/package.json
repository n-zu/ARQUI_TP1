{
  "name": "perf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "artillery": "artillery",
    "node": "artillery run root.yaml -e api",
    "load": "artillery run all.yaml -e api",
    "metar": "artillery run metar.yaml -e api",
    "space_news": "artillery run space_news.yaml -e api",
    "fact": "artillery run fact.yaml -e api",
    "scenario": "./run-scenario.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "artillery": "2.0.0-14",
    "artillery-plugin-statsd": "^2.2.1"
  }
}
